# Le moteur de correction n'a besoin de rien : le dictionnaire francais est
# livre dans « donnees », et tout le reste tient dans la bibliotheque standard.

# Presse-papiers et raccourci clavier global
pyperclip>=1.8
keyboard>=0.13.5

# Icone dans la zone de notification
pystray>=0.19
Pillow>=10.0

# La fenetre : une page web rendue par le moteur d'Edge, present sur toute
# machine depuis Windows 10. Rien n'est telecharge a l'installation.
pywebview>=5.0
pythonnet>=3.0 ; sys_platform == "win32"

# Un clic de souris deplace le curseur, et aucune touche ne le signale :
# sans cette bibliotheque, Papote corrige a partir d'une phrase qui decrit
# un autre endroit du texte. Elle manquait — l'import est sous filet, donc
# l'absence ne se voyait pas.
mouse>=0.7

# -- la dictee --------------------------------------------------------------
# Le micro, et PortAudio qui va avec. Un megaoctet : il reste embarque.
sounddevice>=0.4

# `vosk`, lui, n'est pas ici. Il pese cinquante-quatre megaoctets de
# bibliotheques natives, et Papote est un executable « onefile » : tout ce
# qu'il embarque est extrait a chaque demarrage. Il se telecharge donc au
# premier usage — voir « papote/bibliotheques.py ».
#
# Ces quatre-la, en revanche, sont embarques : `vosk/__init__.py` les
# importe des qu'on le charge, et ils ne pesent rien.
cffi>=1.15
requests>=2.28
srt>=3.5
tqdm>=4.64
